<!DOCTYPE html>
<html>
<head>
    <title>Crash Game</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-align: center;
            padding: 20px;
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .container {
            max-width: 400px;
            background: rgba(0, 0, 0, 0.8);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.5);
        }
        .multiplier {
            font-size: 52px;
            font-weight: bold;
            margin: 25px 0;
            color: #00ff00;
            text-shadow: 0 0 20px #00ff00;
            transition: all 0.3s;
        }
        .bet-form {
            margin: 25px 0;
        }
        input {
            padding: 16px;
            margin: 12px 0;
            border: none;
            border-radius: 12px;
            font-size: 18px;
            width: 90%;
            background: rgba(255,255,255,0.95);
            text-align: center;
        }
        button {
            padding: 18px;
            margin: 12px 0;
            border: none;
            border-radius: 12px;
            font-size: 18px;
            font-weight: bold;
            width: 90%;
            cursor: pointer;
            transition: transform 0.2s;
        }
        button:active {
            transform: scale(0.95);
        }
        #betButton {
            background: #ff6b6b;
            color: white;
        }
        #cashoutBtn {
            background: #4CAF50;
            color: white;
        }
        button:disabled {
            background: #666;
            cursor: not-allowed;
        }
        #status {
            margin: 20px 0;
            font-size: 20px;
            min-height: 30px;
            font-weight: bold;
        }
        .history {
            margin-top: 20px;
            font-size: 14px;
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🎮 CRASH GAME</h1>
        <div class="multiplier" id="multiplier">1.00x</div>
        
        <div class="bet-form">
            <input type="number" id="betAmount" placeholder="💵 Сумма ставки" min="1" value="100">
            <button id="betButton" onclick="placeBet()">🎯 СДЕЛАТЬ СТАВКУ</button>
        </div>
        
        <button id="cashoutBtn" onclick="cashOut()" disabled>💰 ЗАБРАТЬ ВЫИГРЫШ</button>
        <div id="status">🚀 ДЕЛАЙ СТАВКУ! МНОЖИТЕЛЬ РАСТЁТ...</div>
        
        <div class="history" id="history"></div>
    </div>

    <script>
        let tg = window.Telegram.WebApp;
        let multiplier = 1.0;
        let crashed = false;
        let betPlaced = false;
        let crashPoint = 0;
        let gameInterval;
        
        // Инициализация
        tg.expand();
        tg.enableClosingConfirmation();
        startNewRound();
        
        // Новый раунд
        function startNewRound() {
            multiplier = 1.0;
            crashed = false;
            betPlaced = false;
            crashPoint = (Math.random() * 4 + 1.5).toFixed(2);
            document.getElementById('multiplier').innerText = '1.00x';
            document.getElementById('multiplier').style.color = '#00ff00';
            document.getElementById('status').innerText = '🚀 ДЕЛАЙ СТАВКУ! МНОЖИТЕЛЬ РАСТЁТ...';
            document.getElementById('cashoutBtn').disabled = true;
            document.getElementById('betButton').disabled = false;
            
            clearInterval(gameInterval);
            gameInterval = setInterval(updateGame, 100);
        }
        
        // Обновление игры
        function updateGame() {
            if (!crashed) {
                multiplier += 0.01;
                document.getElementById('multiplier').innerText = multiplier.toFixed(2) + 'x';
                
                // Меняем цвет при росте
                if (multiplier > 2) document.getElementById('multiplier').style.color = '#ff9900';
                if (multiplier > 3) document.getElementById('multiplier').style.color = '#ff5500';
                if (multiplier > 4) document.getElementById('multiplier').style.color = '#ff0000';
                
                // Проверка на краш
                if (multiplier >= crashPoint) {
                    crashed = true;
                    document.getElementById('multiplier').style.color = '#ff0000';
                    document.getElementById('status').innerText = '💥 КРАШ! ИГРА ЗАВЕРШИЛАСЬ НА ' + multiplier.toFixed(2) + 'x';
                    setTimeout(startNewRound, 3000);
                }
            }
        }
        
        // Сделать ставку
        function placeBet() {
            const amount = document.getElementById('betAmount').value;
            if (!amount || amount < 1) {
                document.getElementById('status').innerText = '❌ ВВЕДИТЕ СУММУ СТАВКИ!';
                return;
            }
            
            betPlaced = true;
            document.getElementById('cashoutBtn').disabled = false;
            document.getElementById('betButton').disabled = true;
            document.getElementById('status').innerText = '✅ СТАВКА ПРИНЯТА! МНОЖИТЕЛЬ РАСТЁТ...';
        }
        
        // Забрать выигрыш
        function cashOut() {
            const amount = document.getElementById('betAmount').value;
            const winAmount = (amount * multiplier).toFixed(2);
            
            document.getElementById('status').innerText = `🎉 ВЫ ВЫИГРАЛИ ${winAmount} ₽ НА ${multiplier.toFixed(2)}x!`;
            document.getElementById('cashoutBtn').disabled = true;
            
            // Добавляем в историю
            addToHistory(winAmount, multiplier.toFixed(2));
        }
        
        // История игр
        function addToHistory(amount, multiplier) {
            const historyDiv = document.getElementById('history');
            const historyItem = document.createElement('div');
            historyItem.textContent = `🎯 Выигрыш: ${amount} ₽ (x${multiplier})`;
            historyItem.style.margin = '5px 0';
            historyDiv.appendChild(historyItem);
        }
    </script>
</body>
</html>